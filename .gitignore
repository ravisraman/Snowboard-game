node_modules/
dist/
.DS_Store
*.log
.vite/

# Local Claude Code state, and the throwaway git worktrees background agents
# run in. The worktrees carry their own node_modules and are deleted when the
# agent finishes — nothing in here belongs in the repository.
.claude/

# Downloaded elevation tiles, cached by `tools/bake-run.mjs`. Twenty-odd
# megabytes of PNG that the game never loads: what ships is the small table
# the bake writes to `src/world/Elevation.js`, and re-running the bake
# re-fetches these if they are gone.
.dem-cache/
